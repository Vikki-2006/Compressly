# Compressly Frontend Environment Configuration

# Public URL of the deployed FastAPI backend server (e.g. on Render, Railway, Fly.io, or VPS)
# When deploying frontend to Vercel, add VITE_BACKEND_URL to Vercel Environment Variables.
VITE_BACKEND_URL=http://localhost:8000
